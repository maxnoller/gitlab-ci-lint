stages:
  - build
  - test

check-semantic:
  stage: build
  needs: ["non-existent-job"]
  script: echo "Fail"

check-schema:
  stage: test
  script:
    - echo "Fail"
  artifacts:
    path: "wrong-type-should-be-array" 
