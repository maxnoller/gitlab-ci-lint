# GitLab CI Lint

A CLI tool to validate `.gitlab-ci.yml` files offline.

## Features
- Schema validation
- Semantic checks (needs, extends, stages)
- Offline support

## Usage
```bash
gitlab-ci-lint .gitlab-ci.yml
```
